/**
 * Auth Feature - Server-only exports
 * 僅供 Server Components 使用
 */

import "server-only";

// Auth feature 目前沒有獨立的 queries
// 如需添加，在此導出
// export { getUserByEmail } from "./queries/get-user-by-email";
