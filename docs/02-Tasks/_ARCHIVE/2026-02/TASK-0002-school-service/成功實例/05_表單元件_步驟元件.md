# 表單元件 - 步驟元件

## 步驟元件概覽

三個步驟元件各自負責不同的資料收集：

1. **SchoolFormStep** - 學校資料與聯絡人資料
2. **CoursesFormStep** - 課程資料（支援多個課程）
3. **SummaryFormStep** - 總結確認（唯讀顯示）

---

## SchoolFormStep - 學校資料步驟

### 檔案位置

`@/components/feature/school-service/course/SchoolFormStep.tsx`

### 元件職責

- 收集學校基本資料
- 收集合作資料
- 收集聯絡人資料
- 支援選擇現有學校或新增學校
- 自動計算合作學年

### Props 定義

```typescript
interface SchoolFormStepProps {
  schoolData: SchoolBasicData;
  contactData: SchoolContactData;
  onSchoolChange: (data: Partial<SchoolBasicData>) => void;
  onContactChange: (data: Partial<SchoolContactData>) => void;
  errors: Record<string, string>;
  schools: School[];
  isLoadingSchools?: boolean;
  quotationId?: string;
}
```

### 核心功能

#### 1. 學校選擇

```typescript
const handleSchoolSelect = (schoolId: string) => {
  const selectedSchool = schools.find((s) => s.id === schoolId);
  if (selectedSchool) {
    onSchoolChange({
      schoolId,
      schoolName: selectedSchool.schoolName,
    });
  }
};
```

**流程：**

- 使用者從下拉選單選擇現有學校
- 自動填入學校 ID 和名稱
- 其他欄位保持空白，由使用者手動填寫

#### 2. 學年自動計算

```typescript
useEffect(() => {
  if (schoolData.partnershipStartDate && schoolData.partnershipEndDate) {
    const academicYear = calculateAcademicYear(
      schoolData.partnershipStartDate,
      schoolData.partnershipEndDate
    );
    if (academicYear) {
      onSchoolChange({ partnershipStartYear: academicYear });
    }
  }
}, [schoolData.partnershipStartDate, schoolData.partnershipEndDate]);
```

**計算邏輯：**

```typescript
// 範例 1：跨年度
calculateAcademicYear("2026-01-01", "2028-01-02");
// 返回: "2026-2028"

// 範例 2：同年度
calculateAcademicYear("2026-01-01", "2026-12-31");
// 返回: "2026"

// 範例 3：只有開始日期
calculateAcademicYear("2026-01-01", null);
// 返回: "2026"
```

#### 3. 測試數據填充

```typescript
const fillMockData = () => {
  onSchoolChange({
    schoolName: "測試小學",
    schoolNameEn: "Test Primary School",
    address: "九龍測試道123號",
    phone: "21234567",
    email: "info@testschool.edu.hk",
    website: "https://www.testschool.edu.hk",
    partnershipStartDate: "2026-01-01",
    partnershipEndDate: "2028-01-02",
    confirmationChannel: "電郵",
    remarks: "這是一條測試備註",
  });
  onContactChange({
    nameChinese: "陳大文",
    nameEnglish: "Chan Tai Man",
    position: "體育科主任",
    phone: "21234567",
    mobile: "91234567",
    email: "chan@testschool.edu.hk",
  });
};
```

### 表單結構

```typescript
<div className="space-y-6">
  {/* 測試數據按鈕 */}
  <button onClick={fillMockData}>🧪 填充測試數據</button>

  {/* 學校基本資料卡片 */}
  <FormCard title="步驟 1：學校資料">
    {/* 選擇現有學校 */}
    <SearchableSelect
      options={schoolOptions}
      placeholder="搜尋學校或留空新增學校..."
      onChange={handleSchoolSelect}
    />

    {/* 學校名稱 */}
    <Input
      placeholder="例如：聖保羅小學"
      defaultValue={schoolData.schoolName}
      onChange={(e) => onSchoolChange({ schoolName: e.target.value })}
    />

    {/* 其他學校資料欄位... */}
  </FormCard>

  {/* 合作資料卡片 */}
  <FormCard title="合作資料">
    {/* 合作開始日期 */}
    <DatePicker
      onChange={(dates, dateStr) => {
        onSchoolChange({ partnershipStartDate: dateStr });
      }}
    />

    {/* 合作學年（自動計算，唯讀） */}
    <Input
      value={calculateAcademicYear(...)}
      disabled
    />

    {/* 其他合作資料欄位... */}
  </FormCard>

  {/* 聯絡人資料卡片 */}
  <FormCard title="聯絡人資料">
    {/* 聯絡人姓名 */}
    <Input
      placeholder="陳老師"
      defaultValue={contactData.nameChinese}
      onChange={(e) => onContactChange({ nameChinese: e.target.value })}
    />

    {/* 其他聯絡人欄位... */}
  </FormCard>
</div>
```

### 預設選項

```typescript
// 確認渠道選項
const confirmationChannelOptions = [
  { value: "電話", label: "電話" },
  { value: "電郵", label: "電郵" },
  { value: "會議", label: "會議" },
  { value: "WhatsApp", label: "WhatsApp" },
  { value: "面談", label: "面談" },
];

// 職位選項
const positionOptions = [
  { value: "校長", label: "校長" },
  { value: "副校長", label: "副校長" },
  { value: "體育科主任", label: "體育科主任" },
  { value: "課外活動主任", label: "課外活動主任" },
  { value: "主任", label: "主任" },
  { value: "老師", label: "老師" },
  { value: "教練", label: "教練" },
  { value: "職員", label: "職員" },
];
```

---

## CoursesFormStep - 課程資料步驟

### 檔案位置

`@/components/feature/school-service/course/CoursesFormStep.tsx`

### 元件職責

- 管理課程列表（新增/刪除）
- 收集每個課程的資料
- 根據收費模式動態顯示收費欄位
- 提供測試數據填充

### Props 定義

```typescript
interface CoursesFormStepProps {
  courses: CourseItemData[];
  onCoursesChange: (courses: CourseItemData[]) => void;
  errors: Record<string, string>;
}
```

### 核心功能

#### 1. 課程管理

```typescript
// 新增課程
const handleAddCourse = () => {
  onCoursesChange([...courses, getDefaultCourseItem()]);
};

// 刪除課程
const handleRemoveCourse = (id: string) => {
  if (courses.length > 1) {
    onCoursesChange(courses.filter((c) => c.id !== id));
  }
};

// 更新課程
const handleUpdateCourse = (id: string, updates: Partial<CourseItemData>) => {
  onCoursesChange(courses.map((c) => (c.id === id ? { ...c, ...updates } : c)));
};
```

**設計要點：**

- 至少保留一個課程（不允許全部刪除）
- 使用 UUID 作為課程 ID
- 不可變更新模式（immutable update）

#### 2. 動態收費欄位

```typescript
const renderChargingFields = (course: CourseItemData, index: number) => {
  const errorKey = `course_${index}_`;
  const fields = [];

  // 學生每節課堂收費
  if (course.chargingModel.includes(ChargingModel.STUDENT_PER_LESSON)) {
    fields.push(
      <FormField
        key="studentPerLessonFee"
        label="每堂每位學生收費"
        required
        error={errors[`${errorKey}studentPerLessonFee`]}
        hint="學生人數 × 此金額 × 課堂數 = 總收入"
      >
        <AmountInput
          value={course.studentPerLessonFee}
          onChange={(value) =>
            handleUpdateCourse(course.id, { studentPerLessonFee: value })
          }
          placeholder="50"
        />
      </FormField>
    );
  }

  // 導師每堂收費
  if (course.chargingModel.includes(ChargingModel.TUTOR_PER_LESSON)) {
    fields.push(
      <FormField
        key="tutorPerLessonFee"
        label="每堂導師收費"
        required
        error={errors[`${errorKey}tutorPerLessonFee`]}
        hint="此金額 × 課堂數 = 總收入"
      >
        <AmountInput
          value={course.tutorPerLessonFee}
          onChange={(value) =>
            handleUpdateCourse(course.id, { tutorPerLessonFee: value })
          }
          placeholder="800"
        />
      </FormField>
    );
  }

  // ... 其他收費模式

  return <>{fields}</>;
};
```

**收費模式對應表：**

| 收費模式            | 對應欄位             | 計算公式               |
| ------------------- | -------------------- | ---------------------- |
| STUDENT_PER_LESSON  | studentPerLessonFee  | 學生數 × 金額 × 課堂數 |
| TUTOR_PER_LESSON    | tutorPerLessonFee    | 金額 × 課堂數          |
| STUDENT_HOURLY      | studentHourlyFee     | 學生數 × 金額 × 小時數 |
| TUTOR_HOURLY        | tutorHourlyFee       | 金額 × 小時數          |
| STUDENT_FULL_COURSE | studentFullCourseFee | 學生數 × 金額          |
| TEAM_ACTIVITY       | teamActivityFee      | 固定金額               |

#### 3. 測試數據填充

```typescript
const fillMockData = () => {
  if (courses.length > 0) {
    handleUpdateCourse(courses[0].id, {
      courseName: "跳繩恆常班（上學期）",
      courseType: "興趣班",
      courseTerm: CourseTerm.FIRST_TERM,
      requiredTutors: 2,
      maxStudents: 30,
      chargingModel: [
        ChargingModel.STUDENT_PER_LESSON,
        ChargingModel.TUTOR_PER_LESSON,
      ],
      studentPerLessonFee: 50,
      tutorPerLessonFee: 800,
      courseDescription: "適合小三至小五學生，教授基本跳繩技巧及花式跳繩",
    });
  }
};
```

### 表單結構

```typescript
<div className="space-y-6">
  {/* 測試數據按鈕 */}
  <button onClick={fillMockData}>🧪 填充測試數據</button>

  <FormCard title="步驟 2：課程資料">
    <div className="space-y-6">
      {/* 課程列表 */}
      {courses.map((course, index) => (
        <div key={course.id} className="rounded-xl border p-5">
          {/* 課程標題與刪除按鈕 */}
          <div className="flex items-center justify-between">
            <h3>課程 {index + 1}</h3>
            {courses.length > 1 && (
              <button onClick={() => handleRemoveCourse(course.id)}>✕</button>
            )}
          </div>

          {/* 課程欄位 */}
          <div className="space-y-4">
            {/* 課程名稱 */}
            <Input
              placeholder="例如：跳繩恆常班（上學期）"
              defaultValue={course.courseName}
              onChange={(e) =>
                handleUpdateCourse(course.id, { courseName: e.target.value })
              }
            />

            {/* 課程類型、學期、所需導師 */}
            <div className="grid gap-4 sm:grid-cols-3">
              <SearchableSelect
                options={courseTypeOptions}
                placeholder="選擇或輸入課程類型"
                allowCreate={true}
                onChange={(value) =>
                  handleUpdateCourse(course.id, { courseType: value })
                }
              />
              {/* ... */}
            </div>

            {/* 收費模式（多選） */}
            <MultiSelect
              options={chargingModelOptions}
              defaultSelected={course.chargingModel}
              onChange={(selected) =>
                handleUpdateCourse(course.id, { chargingModel: selected })
              }
            />

            {/* 動態收費欄位 */}
            {renderChargingFields(course, index)}

            {/* 課程描述 */}
            <TextArea
              placeholder="例如：適合小三至小五學生..."
              value={course.courseDescription ?? ""}
              onChange={(value) =>
                handleUpdateCourse(course.id, { courseDescription: value })
              }
            />
          </div>
        </div>
      ))}

      {/* 新增課程按鈕 */}
      <button
        onClick={handleAddCourse}
        className="flex w-full items-center justify-center gap-2 rounded-lg border-2 border-dashed py-4"
      >
        + 新增課程
      </button>
    </div>
  </FormCard>
</div>
```

### MultiSelect 使用

```typescript
<MultiSelect
  key={`${course.id}-${course.chargingModel.join(",")}`}
  label=""
  options={chargingModelOptions.map((opt) => ({
    value: opt.value,
    text: opt.label,
    selected: course.chargingModel.includes(opt.value as ChargingModel),
  }))}
  defaultSelected={course.chargingModel}
  onChange={(selected) =>
    handleUpdateCourse(course.id, { chargingModel: selected })
  }
/>
```

**注意事項：**

- 使用 `key` 強制重新渲染（當 chargingModel 變更時）
- `selected` 狀態由父元件控制
- 支援多選

---

## SummaryFormStep - 總結步驟

### 檔案位置

`@/components/feature/school-service/course/SummaryFormStep.tsx`

### 元件職責

- 唯讀顯示所有填寫的資料
- 格式化顯示（電話號碼、收費模式等）
- 提供最後確認機會

### Props 定義

```typescript
interface SummaryFormStepProps {
  formData: NewCourseFormData;
}
```

### 核心功能

#### 1. 電話號碼格式化

```typescript
const formatPhoneNumber = (phone: string) => {
  if (!phone) return "-";

  try {
    const phoneNumber = parsePhoneNumber(phone);
    if (phoneNumber) {
      return phoneNumber.formatInternational();
    }
  } catch (error) {
    // 如果解析失敗，返回原始值
  }

  return phone;
};
```

**範例：**

```typescript
formatPhoneNumber("21234567");
// 返回: "+852 2123 4567"

formatPhoneNumber("91234567");
// 返回: "+852 9123 4567"
```

#### 2. 收費模式顯示

```typescript
const renderChargingModels = (models: ChargingModel[]) => {
  if (!models || models.length === 0) return "-";
  return models.map((model) => CHARGING_MODEL_LABELS[model]).join("、");
};
```

**範例：**

```typescript
renderChargingModels([
  ChargingModel.STUDENT_PER_LESSON,
  ChargingModel.TUTOR_PER_LESSON,
]);
// 返回: "學生每節課堂收費、導師每堂節數收費"
```

#### 3. 收費金額顯示

```typescript
const renderChargingFees = (course: (typeof courses)[0]) => {
  const fees: string[] = [];

  course.chargingModel.forEach((model) => {
    switch (model) {
      case ChargingModel.STUDENT_PER_LESSON:
        if (course.studentPerLessonFee) {
          fees.push(`每堂每位學生：HK$${course.studentPerLessonFee}`);
        }
        break;
      case ChargingModel.TUTOR_PER_LESSON:
        if (course.tutorPerLessonFee) {
          fees.push(`每堂導師：HK$${course.tutorPerLessonFee}`);
        }
        break;
      // ... 其他模式
    }
  });

  return fees.length > 0 ? fees.join("、") : "-";
};
```

**範例：**

```typescript
renderChargingFees({
  chargingModel: [
    ChargingModel.STUDENT_PER_LESSON,
    ChargingModel.TUTOR_PER_LESSON,
  ],
  studentPerLessonFee: 50,
  tutorPerLessonFee: 800,
  // ...
});
// 返回: "每堂每位學生：HK$50、每堂導師：HK$800"
```

### 顯示結構

```typescript
<div className="space-y-6">
  <FormCard title="步驟 3：總結">
    <div className="divide-y">
      {/* 學校資料區塊 */}
      <div className="pb-4">
        <h4>學校資料</h4>
        <InfoRow label="學校名稱" value={school.schoolName} highlight />
        <InfoRow label="地址" value={school.address} />
        <InfoRow label="電話" value={formatPhoneNumber(school.phone)} />
        {/* ... */}
      </div>

      {/* 聯絡人資料區塊 */}
      <div className="py-4">
        <h4>聯絡人資料</h4>
        <InfoRow label="姓名" value={contact.nameChinese} />
        <InfoRow label="電話" value={formatPhoneNumber(contact.phone)} />
        {/* ... */}
      </div>

      {/* 課程資料區塊 */}
      <div className="py-4">
        <h4>課程資料 (學年：{academicYear})</h4>
        {courses.map((course, index) => (
          <div key={course.id} className="mb-4 rounded-lg border p-4">
            <h5>
              課程 {index + 1}：{course.courseName}
            </h5>
            <InfoRow label="課程類型" value={course.courseType} />
            <InfoRow
              label="學期"
              value={COURSE_TERM_LABELS[course.courseTerm]}
            />
            <InfoRow
              label="收費模式"
              value={renderChargingModels(course.chargingModel)}
            />
            <InfoRow label="收費金額" value={renderChargingFees(course)} />
            {/* ... */}
          </div>
        ))}
      </div>
    </div>
  </FormCard>

  {/* 提示訊息 */}
  <div className="rounded-lg bg-brand-50 p-4">
    <p>確認建立後，系統將創建學校資料、聯絡人資料和所有課程。</p>
  </div>
</div>
```

### InfoRow 元件

```typescript
function InfoRow({
  label,
  value,
  highlight = false,
}: {
  label: string;
  value: string | React.ReactNode;
  highlight?: boolean;
}) {
  return (
    <div className="flex items-start justify-between py-2">
      <span className="text-sm text-gray-500">{label}</span>
      <span
        className={`text-sm font-medium ${
          highlight ? "text-brand-600" : "text-gray-800"
        }`}
      >
        {value}
      </span>
    </div>
  );
}
```

## 共用元件

### FormField

```typescript
<FormField
  label="學校名稱"
  required
  error={errors.schoolName}
  hint="請輸入學校的中文名稱"
>
  <Input ... />
</FormField>
```

### FormCard

```typescript
<FormCard title="步驟 1：學校資料" description="填寫學校基本資料和聯絡人資料">
  {/* 表單內容 */}
</FormCard>
```

## 下一步

閱讀 **[06_API_Route.md](./06_API_Route.md)** 了解後端 API 的實作細節。
