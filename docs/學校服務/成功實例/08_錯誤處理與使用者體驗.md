# 錯誤處理與使用者體驗

## 錯誤處理策略

### 多層次錯誤處理

```
前端驗證 (即時反饋)
    ↓
前端提交驗證 (步驟切換)
    ↓
API Route 驗證 (請求驗證)
    ↓
資料庫操作 (Transaction 錯誤)
    ↓
錯誤回傳與顯示
```

## 前端錯誤處理

### 1. 即時驗證

```typescript
const handleSchoolChange = useCallback((updates: Partial<SchoolBasicData>) => {
  setFormData((prev) => {
    const updatedSchool = { ...prev.school, ...updates };

    // 即時清除已修正欄位的錯誤
    setErrors((prevErrors) => {
      const newErrors = { ...prevErrors };
      Object.keys(updates).forEach((key) => {
        delete newErrors[key];
      });

      // 即時驗證日期範圍
      if (
        updatedSchool.partnershipEndDate &&
        updatedSchool.partnershipStartDate &&
        updatedSchool.partnershipEndDate < updatedSchool.partnershipStartDate
      ) {
        newErrors.partnershipEndDate = "結束日期不能早於開始日期";
      }

      return newErrors;
    });

    return {
      ...prev,
      school: updatedSchool,
    };
  });
}, []);
```

**優點：**

- 使用者立即得到反饋
- 減少提交時的錯誤
- 改善使用者體驗

### 2. 步驟驗證

```typescript
const validateStep1 = useCallback((): boolean => {
  const newErrors: Record<string, string> = {};
  const { school, contact } = formData;

  // 必填欄位驗證
  if (!school.schoolName.trim()) {
    newErrors.schoolName = "請輸入學校名稱";
  }
  if (!school.address.trim()) {
    newErrors.address = "請輸入學校地址";
  }

  // 日期範圍驗證
  if (
    school.partnershipEndDate &&
    school.partnershipStartDate &&
    school.partnershipEndDate < school.partnershipStartDate
  ) {
    newErrors.partnershipEndDate = "結束日期不能早於開始日期";
  }

  // 聯絡人驗證
  if (!contact.nameChinese.trim()) {
    newErrors.contactNameChinese = "請輸入聯絡人姓名";
  }

  setErrors(newErrors);
  return Object.keys(newErrors).length === 0;
}, [formData]);
```

**驗證時機：**

- 點擊「下一步」按鈕時
- 驗證失敗時阻止步驟切換
- 顯示所有錯誤訊息

### 3. 提交錯誤處理

```typescript
const handleSubmit = useCallback(async () => {
  setIsSubmitting(true);

  try {
    const response = await fetch(
      "/api/school-service/courses/batch-with-school",
      {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify(requestData),
      }
    );

    if (!response.ok) {
      const error = await response.json();
      throw new Error(error.message || "建立失敗");
    }

    const result = await response.json();
    router.push(`/dashboard/school/courses?schoolId=${result.schoolId}`);
  } catch (error) {
    console.error("Failed to create:", error);
    setErrors({
      submit: error instanceof Error ? error.message : "建立失敗，請稍後再試",
    });
  } finally {
    setIsSubmitting(false);
  }
}, [formData, router]);
```

**錯誤處理流程：**

1. 捕獲 fetch 錯誤
2. 解析後端錯誤訊息
3. 顯示使用者友善的錯誤訊息
4. 確保重置提交狀態

## 錯誤訊息設計

### 1. 欄位錯誤訊息

```typescript
const ERROR_MESSAGES = {
  // 必填欄位
  REQUIRED: "此欄位為必填",
  REQUIRED_SCHOOL_NAME: "請輸入學校名稱",
  REQUIRED_ADDRESS: "請輸入學校地址",
  REQUIRED_CONTACT_NAME: "請輸入聯絡人姓名",

  // 格式錯誤
  INVALID_EMAIL: "請輸入有效的電郵地址",
  INVALID_PHONE: "請輸入有效的電話號碼",
  INVALID_URL: "請輸入有效的網址",

  // 範圍錯誤
  INVALID_DATE_RANGE: "結束日期不能早於開始日期",
  MIN_TUTORS: "至少需要 1 位導師",
  INVALID_AMOUNT: "請輸入有效的收費金額",

  // 業務邏輯錯誤
  NO_COURSES: "請至少新增一個課程",
  NO_CHARGING_MODEL: "請選擇至少一個收費模式",
};
```

### 2. 錯誤訊息顯示

```typescript
// 欄位錯誤
<FormField label="學校名稱" required error={errors.schoolName}>
  <Input
    error={!!errors.schoolName}
    // ...
  />
</FormField>;

// 提交錯誤
{
  errors.submit && (
    <div className="rounded-lg bg-error-50 p-4 text-sm text-error-600 dark:bg-error-900/20 dark:text-error-400">
      <div className="flex items-start gap-3">
        <svg className="h-5 w-5 text-error-600" /* ... */>{/* 錯誤圖示 */}</svg>
        <div>
          <p className="font-medium">建立失敗</p>
          <p className="mt-1">{errors.submit}</p>
        </div>
      </div>
    </div>
  );
}
```

### 3. 錯誤訊息樣式

```typescript
// 欄位錯誤樣式
const inputErrorClass =
  "border-error-500 focus:border-error-500 focus:ring-error-500";
const errorTextClass = "mt-1 text-sm text-error-600 dark:text-error-400";

// 提交錯誤樣式
const submitErrorClass =
  "rounded-lg bg-error-50 p-4 text-sm text-error-600 dark:bg-error-900/20 dark:text-error-400";
```

## 後端錯誤處理

### 1. 請求驗證

```typescript
export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { school, contact, courses } = body;

    // 驗證必要欄位
    if (!school || !contact || !courses || !Array.isArray(courses)) {
      return NextResponse.json({ message: "無效的請求數據" }, { status: 400 });
    }

    // 驗證課程數量
    if (courses.length === 0) {
      return NextResponse.json(
        { message: "請至少新增一個課程" },
        { status: 400 }
      );
    }

    // 繼續處理...
  } catch (error) {
    // 錯誤處理...
  }
}
```

### 2. 資料庫錯誤處理

```typescript
try {
  const result = await prisma.$transaction(async (tx) => {
    // 資料庫操作...
  });
  return NextResponse.json(result);
} catch (error) {
  console.error("Failed to batch create with school:", error);

  // Prisma 錯誤碼處理
  if (error.code === "P2002") {
    return NextResponse.json(
      { message: "學校名稱或電郵已存在" },
      { status: 409 }
    );
  }

  if (error.code === "P2003") {
    return NextResponse.json({ message: "關聯的學校不存在" }, { status: 404 });
  }

  if (error.code === "P2028") {
    return NextResponse.json(
      { message: "資料庫操作超時，請稍後再試" },
      { status: 503 }
    );
  }

  // 一般錯誤
  return NextResponse.json(
    {
      message: "建立失敗",
      error: error instanceof Error ? error.message : "Unknown error",
    },
    { status: 500 }
  );
}
```

### 3. Prisma 錯誤碼對照

| 錯誤碼 | 說明             | HTTP 狀態碼 | 使用者訊息             |
| ------ | ---------------- | ----------- | ---------------------- |
| P2002  | 唯一約束衝突     | 409         | "資料已存在"           |
| P2003  | 外鍵約束失敗     | 404         | "關聯資料不存在"       |
| P2025  | 記錄不存在       | 404         | "找不到指定的資料"     |
| P2028  | Transaction 超時 | 503         | "操作超時，請稍後再試" |

## 使用者體驗優化

### 1. 載入狀態

```typescript
// 學校列表載入
{
  isLoadingSchools ? (
    <div className="h-11 animate-pulse rounded-lg bg-gray-200 dark:bg-gray-700" />
  ) : (
    <SearchableSelect
      options={schoolOptions}
      placeholder="搜尋學校或留空新增學校..."
      onChange={handleSchoolSelect}
    />
  );
}

// 提交載入
<Button onClick={handleSubmit} disabled={isSubmitting}>
  {isSubmitting ? (
    <>
      <svg className="mr-2 h-4 w-4 animate-spin" /* ... */>
        {/* 載入圖示 */}
      </svg>
      建立中...
    </>
  ) : (
    "確認建立課程"
  )}
</Button>;
```

### 2. 防止重複提交

```typescript
const [isSubmitting, setIsSubmitting] = useState(false);

const handleSubmit = useCallback(async () => {
  // 防止重複提交
  if (isSubmitting) return;

  setIsSubmitting(true);

  try {
    // 提交邏輯...
  } finally {
    setIsSubmitting(false);
  }
}, [isSubmitting, formData]);

// 按鈕禁用
<Button onClick={handleSubmit} disabled={isSubmitting}>
  {isSubmitting ? "建立中..." : "確認建立課程"}
</Button>;
```

### 3. 表單狀態指示

```typescript
// 步驟指示器
<StepIndicator
  steps={STEPS}
  currentStep={currentStep}
  onStepClick={handleStepClick}
/>

// 進度提示
<div className="mb-4 text-sm text-gray-500">
  步驟 {currentStep + 1} / {STEPS.length}
</div>
```

### 4. 即時反饋

```typescript
// 欄位驗證反饋
<Input
  type="text"
  value={schoolName}
  onChange={handleChange}
  error={!!errors.schoolName}
  className={errors.schoolName ? "border-error-500" : "border-gray-300"}
/>;
{
  errors.schoolName && (
    <p className="mt-1 text-sm text-error-600">{errors.schoolName}</p>
  );
}

// 成功提示（未來擴展）
{
  showSuccess && (
    <div className="rounded-lg bg-success-50 p-4 text-sm text-success-600">
      <div className="flex items-start gap-3">
        <svg className="h-5 w-5 text-success-600" /* ... */>
          {/* 成功圖示 */}
        </svg>
        <p>課程建立成功！正在跳轉...</p>
      </div>
    </div>
  );
}
```

### 5. 測試數據填充

```typescript
const fillMockData = () => {
  onSchoolChange({
    schoolName: "測試小學",
    schoolNameEn: "Test Primary School",
    address: "九龍測試道123號",
    phone: "21234567",
    email: "info@testschool.edu.hk",
    website: "https://www.testschool.edu.hk",
    partnershipStartDate: "2026-01-01",
    partnershipEndDate: "2028-01-02",
    confirmationChannel: "電郵",
    remarks: "這是一條測試備註",
  });
  // ...
};

// 測試按鈕
<button
  type="button"
  onClick={fillMockData}
  className="rounded-md bg-gray-100 px-3 py-1 text-sm text-gray-600 hover:bg-gray-200"
>
  🧪 填充測試數據
</button>;
```

## 錯誤恢復策略

### 1. 草稿儲存（未來擴展）

```typescript
// 自動儲存草稿
useEffect(() => {
  const timer = setTimeout(() => {
    localStorage.setItem("courseDraft", JSON.stringify(formData));
  }, 1000);

  return () => clearTimeout(timer);
}, [formData]);

// 載入草稿
useEffect(() => {
  const draft = localStorage.getItem("courseDraft");
  if (draft) {
    const shouldRestore = confirm("發現未完成的表單，是否恢復？");
    if (shouldRestore) {
      setFormData(JSON.parse(draft));
    }
  }
}, []);
```

### 2. 重試機制

```typescript
const handleSubmitWithRetry = async (retries = 3) => {
  for (let i = 0; i < retries; i++) {
    try {
      const response = await fetch("/api/...", { ... });
      if (response.ok) {
        return await response.json();
      }
    } catch (error) {
      if (i === retries - 1) {
        throw error;
      }
      // 等待後重試
      await new Promise(resolve => setTimeout(resolve, 1000 * (i + 1)));
    }
  }
};
```

### 3. 離線處理（未來擴展）

```typescript
// 檢測網路狀態
const [isOnline, setIsOnline] = useState(navigator.onLine);

useEffect(() => {
  const handleOnline = () => setIsOnline(true);
  const handleOffline = () => setIsOnline(false);

  window.addEventListener("online", handleOnline);
  window.addEventListener("offline", handleOffline);

  return () => {
    window.removeEventListener("online", handleOnline);
    window.removeEventListener("offline", handleOffline);
  };
}, []);

// 離線提示
{
  !isOnline && (
    <div className="rounded-lg bg-warning-50 p-4 text-sm text-warning-600">
      網路連線中斷，請檢查網路連線後再試
    </div>
  );
}
```

## 日誌記錄

### 1. 前端日誌

```typescript
// 開發環境
if (process.env.NODE_ENV === "development") {
  console.log("Form data:", formData);
  console.log("Validation errors:", errors);
}

// 錯誤追蹤（生產環境）
try {
  // 操作...
} catch (error) {
  // 發送到錯誤追蹤服務（如 Sentry）
  if (typeof window !== "undefined" && window.Sentry) {
    window.Sentry.captureException(error, {
      tags: {
        component: "NewCourseForm",
        action: "submit",
      },
      extra: {
        formData,
      },
    });
  }

  console.error("Failed to create:", error);
}
```

### 2. 後端日誌

```typescript
// 請求日誌
console.log("POST /api/school-service/courses/batch-with-school", {
  schoolName: school.schoolName,
  coursesCount: courses.length,
  timestamp: new Date().toISOString(),
});

// 錯誤日誌
console.error("Failed to batch create with school:", {
  error: error.message,
  stack: error.stack,
  schoolName: school.schoolName,
  timestamp: new Date().toISOString(),
});

// 成功日誌
console.log("Successfully created:", {
  schoolId: result.schoolId,
  coursesCount: result.coursesCount,
  timestamp: new Date().toISOString(),
});
```

## 使用者引導

### 1. 提示訊息

```typescript
// 欄位提示
<FormField
  label="學校名稱"
  required
  hint="請輸入學校的中文全名"
>
  <Input ... />
</FormField>

// 收費模式提示
<FormField
  label="收費模式"
  required
  hint="可選擇多個收費模式"
>
  <MultiSelect ... />
</FormField>
```

### 2. 說明文字

```typescript
// 總結步驟說明
<div className="rounded-lg bg-brand-50 p-4 dark:bg-brand-900/20">
  <div className="flex items-start gap-3">
    <svg className="mt-0.5 h-5 w-5 text-brand-600" /* ... */>
      {/* 資訊圖示 */}
    </svg>
    <div>
      <p className="text-sm font-medium text-brand-800 dark:text-brand-300">
        下一步操作
      </p>
      <p className="mt-1 text-sm text-brand-600 dark:text-brand-400">
        確認建立後，系統將創建學校資料、聯絡人資料和所有課程。
        您可以在課程詳情頁面進行排課和分配導師。
      </p>
    </div>
  </div>
</div>
```

### 3. 工具提示（未來擴展）

```typescript
<Tooltip content="學年會根據合作開始日期自動計算">
  <InfoIcon className="h-4 w-4 text-gray-400" />
</Tooltip>
```

## 無障礙設計

### 1. ARIA 標籤

```typescript
<Input
  type="text"
  aria-label="學校名稱"
  aria-required="true"
  aria-invalid={!!errors.schoolName}
  aria-describedby={errors.schoolName ? "schoolName-error" : undefined}
/>;
{
  errors.schoolName && (
    <p id="schoolName-error" role="alert">
      {errors.schoolName}
    </p>
  );
}
```

### 2. 鍵盤導航

```typescript
// 步驟切換快捷鍵
useEffect(() => {
  const handleKeyDown = (e: KeyboardEvent) => {
    if (e.ctrlKey || e.metaKey) {
      if (e.key === "ArrowLeft") {
        handlePrev();
      } else if (e.key === "ArrowRight") {
        handleNext();
      }
    }
  };

  window.addEventListener("keydown", handleKeyDown);
  return () => window.removeEventListener("keydown", handleKeyDown);
}, [handlePrev, handleNext]);
```

### 3. 焦點管理

```typescript
// 步驟切換後聚焦第一個輸入框
useEffect(() => {
  const firstInput = document.querySelector(
    "input:not([disabled])"
  ) as HTMLInputElement;
  if (firstInput) {
    firstInput.focus();
  }
}, [currentStep]);
```

## 效能監控

### 1. 表單提交時間

```typescript
const handleSubmit = useCallback(async () => {
  const startTime = performance.now();

  try {
    // 提交邏輯...

    const endTime = performance.now();
    console.log(`Submit took ${endTime - startTime}ms`);
  } catch (error) {
    // 錯誤處理...
  }
}, [formData]);
```

### 2. 渲染效能

```typescript
// 使用 React DevTools Profiler
import { Profiler } from 'react';

<Profiler id="NewCourseForm" onRender={onRenderCallback}>
  <NewCourseForm ... />
</Profiler>
```

## 總結

完善的錯誤處理與使用者體驗優化包括：

1. **多層次驗證** - 即時驗證、步驟驗證、提交驗證
2. **友善的錯誤訊息** - 清晰、具體、可操作
3. **載入狀態** - 防止重複提交、顯示進度
4. **錯誤恢復** - 草稿儲存、重試機制
5. **使用者引導** - 提示訊息、說明文字
6. **無障礙設計** - ARIA 標籤、鍵盤導航
7. **日誌記錄** - 前端日誌、後端日誌

這些實踐確保了良好的使用者體驗和系統可靠性。
